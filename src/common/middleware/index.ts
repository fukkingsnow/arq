/**
 * Barrel export for middleware module
 * Phase 24 Backend Middleware Layer
 * Centralized middleware exports for HTTP request/response processing
 */

export { RequestLoggingMiddleware } from './request-logging.middleware';
export { CompressionRequestMiddleware } from './compression-request.middleware';
