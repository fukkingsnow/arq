version: '3.8'

services:
  arq:
    build:
      context: .
      dockerfile: deployment/Dockerfile.prod
    container_name: arq-api
    image: arq:latest
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "3000:8000" # Направляем внешний 3000 на внутренний 8000
    environment:
      - NODE_ENV=production
      - LOG_LEVEL=info
      - DATABASE_URL=postgresql://arq:${DB_PASSWORD}@arq-postgres:5432/arq
      - REDIS_URL=redis://arq-redis:6379
      - JWT_SECRET=${JWT_SECRET:-supersecretchangeit}
      - API_PORT=8000
    restart: always
    healthcheck:
      # Проверяем порт 8000, который реально открыт в приложении
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8000/api/v1/health', (r) => { if (r.statusCode < 500) process.exit(0); else process.exit(1); })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - arq-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgres:14-alpine
    container_name: arq-postgres
    environment:
      - POSTGRES_DB=arq
      - POSTGRES_USER=arq
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    restart: always
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - arq-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arq -d arq"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: arq-redis
    restart: always
    volumes:
      - redis-data:/data
    networks:
      - arq-network
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  ollama:
    image: ollama/ollama:latest
    container_name: arq-ollama
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - arq-network
    restart: always

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local
  ollama-data:
    driver: local

networks:
  arq-network:
    driver: bridge
